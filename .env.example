# TDX (Transport Data eXchange)
# Copy this file to `.env.local` when live CCTV is ready.
# Leave empty to use the bundled SNAPSHOT (Tainan city + nearby freeway).
# Do not commit real secrets.

NEXT_PUBLIC_TDX_CLIENT_ID=
NEXT_PUBLIC_TDX_CLIENT_SECRET=
